<?xml version="1.0"?>
<ruleset name="CustomRuleset">
    <description>Custom ruleset for the project.</description>
    <rule ref="PSR12">
        <!-- Exclude the rule for line length -->
        <exclude name="Generic.Files.LineLength"/>
    </rule>
</ruleset>